import React, { useState, useEffect } from 'react';
import axios from 'axios';
import Comment from '../components/comment';  // Import the Comment component

function CommentsSection({ blogId, loggedInUser }) {
    // State and useEffect for fetching comments
    // State for new comment
    // Function to handle posting comments
    // Render comments using Comment component

    return (
        <div className="comments-section">
            {/* Render comments */}
            {/* Render new comment form */}
        </div>
    );
}

export default CommentsSection;